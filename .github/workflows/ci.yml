name: CI

on:
  push:
    branches: [main, kp]
  pull_request:

permissions:
  contents: read

env:
  NODE_AUTH_TOKEN: ${{ secrets.OMNIXYS_TOKEN }}
  NODE_ENV: production

  NEXT_PUBLIC_OTEL_ENDPOINT: asd #${{ vars.NEXT_PUBLIC_OTEL_ENDPOINT }}
  NEXT_PUBLIC_OTEL_SERVICE_NAME: asd #${{ vars.NEXT_PUBLIC_OTEL_SERVICE_NAME }}
  NEXT_PUBLIC_OTEL_SAMPLE_RATE: asd #${{ vars.NEXT_PUBLIC_OTEL_SAMPLE_RATE }}

  NEXT_PUBLIC_BACKEND_SERVER_URL: asd #${{ vars.NEXT_PUBLIC_BACKEND_SERVER_URL }}
  NEXT_PUBLIC_GRAPHQL_WS_URL: asd #${{ vars.NEXT_PUBLIC_GRAPHQL_WS_URL }}

  NEXT_PUBLIC_FALLBACK_URL: asd #${{ vars.NEXT_PUBLIC_FALLBACK_URL }}
  NEXT_PUBLIC_CHECKPOINT_BASE_PATH: asd #${{ vars.NEXT_PUBLIC_CHECKPOINT_BASE_PATH }}

  NEXT_PUBLIC_EVENT_ID: asd #${{ secrets.NEXT_PUBLIC_EVENT_ID }}

  NEXT_PUBLIC_BASE_URL: asd #${{ vars.NEXT_PUBLIC_BASE_URL }}
  NEXT_PUBLIC_APP_URL: asd #${{ vars.NEXT_PUBLIC_APP_URL }}
  NEXT_PUBLIC_NEXYS_HOME_URL: asd #${{ vars.NEXT_PUBLIC_NEXYS_HOME_URL }}
  NEXT_PUBLIC_OMNIXYS_TENANT_ID: asd #${{ vars.NEXT_PUBLIC_OMNIXYS_TENANT_ID }}

  NEXT_PUBLIC_EVENT_API: asd #${{ vars.NEXT_PUBLIC_EVENT_API }}
  NEXT_PUBLIC_INVITATION_API: asd #${{ vars.NEXT_PUBLIC_INVITATION_API }}

  # Server-only env vars (required at build time for server modules)
  MAIL_TOKEN_URL: asd #${{ secrets.MAIL_TOKEN_URL }}
  STALWART_JMAP_URL: asd #${{ secrets.STALWART_JMAP_URL }}
  ADMIN_USERNAME: asd #${{ secrets.ADMIN_USERNAME }}
  ADMIN_PASSWORD: asd #${{ secrets.ADMIN_PASSWORD }}
  OMNIMAIL_SERVICE_TOKEN: asd #${{ secrets.OMNIMAIL_SERVICE_TOKEN }}
  OMNIXYS_TENANT_ID: asd #{{ secrets.OMNIXYS_TENANT_ID }}
  ANALYTICS_CONSENT_SECRET: asd #${{ secrets.ANALYTICS_CONSENT_SECRET }}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.33.0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          registry-url: https://npm.pkg.github.com
          scope: "@omnixys"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Test
        run: pnpm test

      - name: Typecheck
        run: pnpm exec tsc --noEmit

      - name: Lint
        run: pnpm exec biome check .

      - name: Build
        run: pnpm build
